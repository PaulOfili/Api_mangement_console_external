const styles = theme => ({
    custom_checkbox: {
        height: '1.5rem',
        width: '1.5rem',
        transform: 'scale(1.5)',
    },

    custom_checkbox_label: {
        marginBottom: 0,
    }
});


export default styles;